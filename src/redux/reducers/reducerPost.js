import {GET_USER_ID, GET_LIST_OF_POSTS} from "constants/actionsConstants"

const initialState = {
    userId: undefined,
    //title: undefined,
    // text: undefined,

};

export default function reducerNotes(state = initialState, action) {
    switch (action.type) {

        default:
            return state
    }
}